Dog = class() ref self {
    self.build_ = func(name) {
        self.name = name;
        self.weight = 40;
    };

    self.equals_ = func(other) {
        ret self.name == other.name & self.weight == other.weight;
    };

    self.bark = func() {
        _prt self.name + " : Bark!";
    };
};

moki = Dog("Moki");

moki.bark();

_prt "----------------";

_prt moki == dog("Moki");